{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_resetting_reset', {'token': token}), 'attr': { 'class': 'fos_user_resetting_reset' } }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'resetting.reset.submit'|trans }}" />
    </div>
        
        
        
{{ form_end(form) }}
Este Formulario Fea, para poner la nueva contraseña


{#
<div class="row">
        <div class="col-md-6 col-md-offset-3">
            {{ form_start(form, { 'action': path('fos_user_resetting_reset', {'token': token}), 'attr': { 'class': 'fos_user_resetting_reset' } }) }}
            
            
                <div id="divStartP" class="form-group">
                    <p>Nueva Contraseña</p>
                    {{ form_errors(form.new_password) }}
                    <div class="input-group">
                        {{ form_widget(form.new_password, {'attr': {'class': 'form-control  input-md'} }) }}
                        <span class="input-group-addon"><i class="fa fa-key" aria-hidden="true"></i></span>    
                    </div>
                    <span id="helpBlockStartP" class="help-block"></span>
                </div>

                <div id="divAdvanceP" class="form-group">
                    <p>Confirmar Nueva Contraseña</p>
                    {{ form_errors(form.new_password_confirmation) }}
                    <div class="input-group">
                        {{ form_widget(form.new_password_confirmation, {'attr': {'class': 'form-control  input-md'} }) }}
                        <span class="input-group-addon"><i class="fa fa-key" aria-hidden="true"></i></span>
                    </div>
                    <span id="helpBlockAdvanceP" class="help-block"></span>
                </div>

            <div>
                <input class="btn btn-success btn-block" type="submit" value="{{ 'Guardar'|trans }}" />
            </div>
            {{ form_end(form) }}    
        </div>
    </div>

#}